const reusableButton = ({ children }) => {
    return (<div className="btn-container"><button className="home-btn" >{children}</button></div>)



};

export default reusableButton;

// a function for a reusable button that I have used several times in this project